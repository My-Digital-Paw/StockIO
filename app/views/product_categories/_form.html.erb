<%= form_for(@product_category) do |f| %>
  <% if @product_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@product_category.errors.count, "error") %> prohibited this product_category from being saved:</h2>

      <ul>
      <% @product_category.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :parent_category %><br />
    <%= f.collection_select(:parent_category_id, ProductCategory.all, :id, :name, { :include_blank => "---" }) %>
    <!--<%= f.text_field :parent_category %>-->
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
